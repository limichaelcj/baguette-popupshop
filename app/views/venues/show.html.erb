<div class="banner" style="background-image: url(<%= @venue.photo %>)">
<div class="venueTitle">
<span style="text-transform:uppercase; font-size: 22px;"><%= @venue.name %></span><br>
<%= @venue.address %><br>
<i class="fas fa-yen-sign"></i> <%= @venue.price %> / event
</div>

<a href="/venues">
<div class="back">
VENUES LIST
</div>
</a>

<div class="calendar">
<%= render 'calendar_form' %>
</div>

</div>

<div class="information">
<p>
<%= @venue.description %>
</p>
</div>

<div class="venueMap">
  <%= render 'shared/map' %>
</div>

<!-- needs major restyling, add class:"datepicker" to input -->



